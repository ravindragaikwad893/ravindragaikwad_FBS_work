name:Ravindra ajinath Gaikwad
set-A
roll no-03




Q2

#include <stdio.h>

int main() {
    char str[100];
    char oldChar, newChar;

    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin);
    
    str[strcspn(str, "\n")] = '\0';

    printf("Enter character to replace: ");
    scanf(" %c", &oldChar);

    printf("Enter special symbol (e.g. @ # $ *): ");
    scanf(" %c", &newChar);


    for (int i = 0; str[i]!= '\0'; i++) {
        if (str[i] == oldChar) {
            str[i] = newChar;
        }
    }

    printf("\nModified string: %s\n", str);

    return 0;
    
}




Q1

#include <stdio.h>
#include <stdio.h>

int isPrime(int n) {
    if (n < 2) return 0;
    if (n == 2) return 1;
    if (n % 2 == 0) return 0;
    for (int i = 3; i <= sqrt(n); i += 2) {
        if (n % i == 0) return 0;
    }
    return 1;
}

int main() {
    int start, end, sum = 0;

    printf("Enter start of range: ");
    scanf("%d", &start);
    printf("Enter end of range: ");
    scanf("%d", &end);

    printf("Prime numbers between %d and %d are ", start, end);

    for (int i = start; i <= end; i++) {
        if (isPrime(i)) {
            printf("%d ", i);
            sum += i;
        }
    }

    printf("\nSum of prime numbers = %d\n", sum);

    return 0;
}



